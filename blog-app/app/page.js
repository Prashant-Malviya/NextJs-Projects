import Home from '@/components/Home'

export const metadata = {
  title: "Homepage",
};

const Homepage = async() => {



  return (
    <div>
      <Home />
    </div>
  );
};

export default Homepage;
